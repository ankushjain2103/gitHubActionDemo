{
  "name": "kohler",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean-reports": "rm -R -f cypress/reports/html",
    "report:merge": "mochawesome-merge cypress/reports/html/*.json -o cypress/reports/html/report.json",
    "report:generate": "marge cypress/reports/html/report.json --charts true",
    "scripts-execution-prod": "cypress run --browser chrome --spec 'cypress/e2e/**.cy.js' --headed "
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "-": "^0.0.1",
    "cypress": "^13.8.1"
  }
}